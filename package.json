{
  "name": "xbrowser",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cd src && npm run dev",
    "build": "cd src && npm run build",
    "tauri": "tauri",
    "tauri:dev": "cd src && npm run dev & tauri dev",
    "tauri:build": "cd src && npm run build && tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  }
}
